<h1><%= @genre.name %></h1>
<ul>
  <% @recent_pages.each do |page| %>
    <li><%= page.title %></li>
  <% end %>
</ul>
<a href="<%= genre_pages_url(@genre.id) %>">See more <%= @genre.name %> Genius</a>
